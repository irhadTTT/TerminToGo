services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: termintogo
      POSTGRES_PASSWORD: irhad
      POSTGRES_DB: termin_to_go
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: